/** @format */

import Container from "@/ui/components/container/container";
import Image from "next/image";
import React from "react";
import Typography from "@/ui/designSystem/typography/typography";
import Button from "@/ui/designSystem/button/button";
import { useRouter } from "next/router";
import Collection from "./decouvir/collection";

const statistiques = [
  { count: "200+", label: "Marques internationales" },
  { count: "2,000+", label: "Produits de haute qualité" },
  { count: "200+", label: "Clients satisfaits" },
];

export default function Decouvrir() {
  const router = useRouter();

  // Navigation vers la boutique
  const pageShop = () => {
    router.push("/shop");
  };

  return (
    <div className="bg-white">
      {/* Section Desktop */}
      <Container className="flex flex-wrap lg:flex-nowrap overflow-visible">
        <div className="md:flex-1 space-y-6 relative p-2 md:p-10">
          {/* Titre principal */}
          <Typography variant="h3" className="text-gray-900 font-bold">
            TROUVEZ VOTRE PRODUIT FAVORI EN FONCTION
          </Typography>

          {/* Description */}
          <Typography variant="body" className="text-gray-600 m-0">
            Découvrez notre vaste collection de produits soigneusement conçus,
            pensés pour révéler votre individualité et répondre à votre sens du
            style.
          </Typography>
          <div className="hidden md:flex">
            {/* Bouton Boutique */}
          <Button
            variant="accent"
            size="medium"
            className="mt-4 rounded-full  transition-transform transform hover:scale-105"
            action={pageShop}
          >
            Visitez la boutique
          </Button>
          </div>
          <div className="md:hidden">
            {/* Bouton Boutique */}
          <Button
            variant="accent"
            size="medium"
            className=" rounded-full w-full transition-transform transform hover:scale-105"
            action={pageShop}
          >
            Visitez la boutique
          </Button>
          </div>
          

          {/* Statistiques */}
          <div className="grid grid-cols-2 sm:grid-cols-3 gap-6 sm:mt-8">
            {statistiques.map((stat, index) => (
              <div key={index} className="text-center">
                <Typography variant="h3" className="text-gray-900 font-bold">
                  {stat.count}
                </Typography>
                <Typography
                  variant="caption3"
                  theme="gray"
                  className="text-gray-500"
                >
                  {stat.label}
                </Typography>
              </div>
            ))}
          </div>
        </div>

        {/* Image */}
        <div className="absolute w-full lg:w-1/2 mt-6 lg:mt-0 hidden">
          <div className="relative w-full h-64 lg:h-full">
            <Image
              src="/assets/images/accueils.jpg"
              alt="Decouvrir"
              layout="fill"
              objectFit="contain"
              objectPosition="center"
              className="rounded-lg border-4 border-white shadow-lg transition-transform transform hover:scale-105"
            />
          </div>
        </div>
      </Container>

      {/* Section Mobile */}
      {/* <Container className="relative h-screen lg:hidden overflow-hidden">
  <Image
    src="/assets/images/Slide1.png"
    alt="Decouvrir"
    layout="fill" // L'image occupe toute la taille du conteneur
    objectPosition="center" // Centre l'image dans le conteneur
    objectFit="contain" // Pas de zoom, l'image s'ajuste dans l'espace
    className="absolute"
  />
  <Collection className="absolute p-5 bg-white bg-opacity-80 rounded-lg shadow-lg bottom-10 right-10" />
</Container> */}

    </div>
  );
}
